<!-- Generated by scripts/gen-tool-reference.mjs. Do not edit by hand — run `npm run docs:reference`. -->

# `plan.*`

18 tools in the `plan` namespace.

## `plan.program.create`

Create a new training program — the top-level container for a plan (holds one or more blocks/mesocycles).

Start here when scaffolding a new plan from scratch.

**Parameters**

- `id` — `string`, optional.
- `name` — `string`, **required**.
- `description` — `string`, optional.

## `plan.program.list`

List existing programs.

**Parameters**

- `includeArchived` — `boolean`, optional.

## `plan.program.get`

Fetch one program by id.

**Parameters**

- `id` — `string`, **required**.

## `plan.program.archive`

Archive a program (soft-retire it from active use without deleting its history).

**Parameters**

- `id` — `string`, **required**.

## `plan.block.create`

Create a training block (mesocycle) under a program — takes the parent programId.

A block holds one or more weeks. Passing the `id` of an EXISTING block updates it in place; if that raises `weeksCount` after weeks were already built under it, `warnings[]` carries a `meso_length_grew_mid_block` advisory (VMCP-06.03 / B32). Never blocks the write.

**Parameters**

- `id` — `string`, optional.
- `programId` — `string`, **required**.
- `orderIndex` — `integer` (min 0), **required**.
- `name` — `string`, **required**.
- `focus` — `string`, optional.
- `weeksCount` — `integer` (min 1), **required**.
- `notes` — `string`, optional.

## `plan.block.list_for_program`

List the blocks belonging to one program (takes programId).

**Parameters**

- `programId` — `string`, **required**.

## `plan.week.create`

Create a week under a block — takes the parent blockId.

A week holds one or more workout templates.

**Parameters**

- `id` — `string`, optional.
- `blockId` — `string`, **required**.
- `orderIndex` — `integer` (min 0), **required**.
- `name` — `string`, optional.

## `plan.week.list_for_block`

List the weeks belonging to one block (takes blockId).

**Parameters**

- `blockId` — `string`, **required**.

## `plan.template.create`

Create a workout template under a week — takes the parent weekId.

A template holds one or more planned exercises and is what `plan.next_workout`/`plan.complete_workout` operate on. Returns `warnings: []` — a template holds no volume until exercises are added, so the tier-aware volume lints run on `plan.exercise.create`, not here.

**Parameters**

- `id` — `string`, optional.
- `weekId` — `string`, **required**.
- `dayLabel` — `string`, optional.
- `name` — `string`, **required**.
- `notes` — `string`, optional.
- `orderIndex` — `integer` (min 0), **required**.

## `plan.template.get`

Fetch one workout template by id.

**Parameters**

- `id` — `string`, **required**.

## `plan.template.list_for_week`

List the workout templates belonging to one week (takes weekId).

**Parameters**

- `weekId` — `string`, **required**.

## `plan.exercise.create`

Add a planned exercise to a workout template — takes the parent workoutTemplateId.

This is the leaf of the plan hierarchy: the actual prescribed exercise/sets/reps/load for one slot in one template. Also returns `warnings[]`: tier-aware RP volume ceilings re-checked over the WHOLE template after the insert (sets per exercise, and hard sets per muscle per session, counted on each exercise's PRIMARY muscle group only), PLUS three cross-template checks over the rest of the week (hard sets per muscle per week, the same muscle over the per-session ceiling on two consecutive-orderIndex templates, and the priority muscle drifting between week 1 and a later week of the same block — VMCP-06.03 / B32). Each warning is a SUGGESTION; accept or decline it, and never re-apply it after a decline. The write ALWAYS succeeds — a warning never blocks, never rolls back, and never edits the row you just created. Read a warning out to the lifter and offer the fix it names; if they decline, drop it and move on. `targetTempo` (VW-46) is an optional coach-set tempo override — `{ ecc, pauseBottom, con, pauseTop }` seconds, each >= 0 — that wins over the exercise/movement-pattern default when the live prescription resolves a tempo; omit it to leave the default in effect.

**Parameters**

- `id` — `string`, optional.
- `workoutTemplateId` — `string`, **required**.
- `exerciseId` — `string`, **required**.
- `orderIndex` — `integer` (min 0), **required**.
- `targetSets` — `integer` (min 1), **required**.
- `targetRepsLow` — `integer` (min 1), optional.
- `targetRepsHigh` — `integer` (min 1), optional.
- `targetWeightLbs` — `number` (min 0), optional.
- `targetRpe` — `number` (0–10), optional.
- `restSec` — `integer` (min 0), optional.
- `notes` — `string`, optional.
- `targetTempo` — `object`, optional.

## `plan.exercise.list_for_template`

List the planned exercises belonging to one workout template (takes workoutTemplateId).

**Parameters**

- `workoutTemplateId` — `string`, **required**.

## `plan.next_workout`

Get the next un-completed workout template for a program (or the active/default program if programId is omitted).

Use this to answer "what should the user do today per their plan?" Returns `blockBoundary: null` unless the returned template is the first of a new block (VMCP-06.06 / B48), in which case it carries the finished block, the new block, the current goal on file, and an advisory prompt to keep or restate that goal — never auto-applied, and the goal itself is never written by this tool.

**Parameters**

- `programId` — `string`, optional.

## `plan.complete_workout`

Mark a workout template as completed, optionally linking the real session that completed it (sessionId).

Advances what `plan.next_workout` returns next. Returns `blockBoundary: null` unless the completed template is the last template of the last week in its block (VMCP-06.06 / B48), in which case it carries the finished block, the next block (or null if none), the current goal on file, and an advisory goal-realignment prompt — never auto-applied, and the goal itself is never written by this tool.

**Parameters**

- `workoutTemplateId` — `string`, **required**.
- `sessionId` — `string`, optional.

## `plan.attach_to_session`

Link a live/real session to a plan entity — either a specific plannedExerciseId or a whole workoutTemplateId (exactly one of the two).

Use this to connect what the user is actually doing right now to what the plan prescribed, without requiring the session to have been started from the plan in the first place.

**Parameters**

- `sessionId` — `string`, **required**.
- `plannedExerciseId` — `string`, optional.
- `workoutTemplateId` — `string`, optional.

## `plan.suggest_progression`

Get a suggested load/weight-delta for the next occurrence of an exercise, based on the most recently completed session for it (completedSessionId, optional — inferred if omitted) within a program.

Returns `delta` (lb) plus `repDelta` — above a ~15-rep prescription the rep is the finer dial than the load, so the suggestion adds a rep instead of weight. `gates` reports the ordered progression gates (technique, effort, setsUnlocked: whether adding a set is warranted) and `tier` the training-experience signal the gates were read against. Suggestion only: the coach or lifter accepts or declines it, it is never auto-applied, and a declined suggestion is not re-applied.

**Parameters**

- `programId` — `string`, optional.
- `exerciseId` — `string`, **required**.
- `completedSessionId` — `string`, optional.
- `lifter` — `string`, optional.

## `plan.warmup_ramp`

Propose a warm-up ramp for one exercise at one working load.

READ-ONLY: it returns rows, writes nothing, and never starts a set — you start each row yourself with `set.start { setPurpose: "warmup" }`, and you may skip or shorten the ramp if the lifter says they are already warm. The ramp is RP's: 12 reps at roughly 30RM, 8 at roughly 20RM, 4 at roughly 10RM before the FIRST exercise for a muscle group, then a single 3-6 rep feel set at the working load for later exercises on a muscle that is already warm (call `coaching.explain { topic: "live.warmup_protocol" }` for the sourced prose). Each row carries its OWN coaching focus — cadence, then position, then bracing and eccentric control — rather than generic "warming up" copy. Loads are derived from `workingWeightLbs` via the e1RM helpers, rounded DOWN to the device load step and never above the working load. THE LOADS ARE A POPULATION ESTIMATE, NOT PERSONALISED: you give a load but not the reps the lifter gets on it, so the ramp assumes the working load is about a 5RM and derives the 30/20/10RM rungs from that — roughly 58/70/88% of the day's working load. That reads deliberately light, which is the safe direction for a warm-up; if the lifter says a rung felt too easy or too heavy, believe them over these numbers. `feelSetOnly: true` means the muscle was already warmed by an earlier exercise in the active session, and `reason` says which. The experience tier changes only the set COUNT (a beginner gets one extra practice rung, an advanced lifter drops the 12-rep rung); the percentages are tier-invariant. NOTE the tier signal cannot report `advanced` yet — its derived ceiling stops at `intermediate` — so today you'll see the 3-rung or 4-rung shape only. Per-lifter, per-exercise deviations are NOT modelled yet — that needs the exercise baseline at PROVISIONAL — so treat the count as a population starting point and adjust on what the lifter tells you.

**Parameters**

- `exerciseId` — `string`, **required**.
- `workingWeightLbs` — `number`, **required**.
- `slot` — `string`, optional. Device slot identifier. Defaults to 'primary' for single-device sessions. Used to disambiguate when multiple devices are connected (e.g., 'left' / 'right' for bilateral exercises). Must match /^[a-zA-Z][a-zA-Z0-9_-]\*$/ — letters, digits, underscores, and hyphens, leading with a letter.
