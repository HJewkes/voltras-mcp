<!-- Generated by scripts/gen-tool-reference.mjs. Do not edit by hand — run `npm run docs:reference`. -->

# `truecoach.*`

1 tool in the `truecoach` namespace.

## `truecoach.import_week`

Pull the coach-assigned workouts for a date range out of TrueCoach and upsert them into the local plan.\* tree (program -> "TrueCoach import" block -> one week per ISO week -> workout template -> planned exercises).

READ-ONLY against TrueCoach: this tool never writes, comments, or logs results there, and it runs only when you call it — there is no background sync. Credentials come from VMCP_TRUECOACH_USERNAME plus VMCP_TRUECOACH_PASSWORD or VMCP_TRUECOACH_PASSWORD_CMD; with none set it returns NOT_CONFIGURED and makes no network call. Idempotent — re-importing the same range updates rows in place via TrueCoach external ids and never duplicates. Exercise names must match the catalog exactly; anything else is reported in `unmapped` with candidates and skipped (the template still lands), and you can resolve it by passing `mapping: { "<TrueCoach name>": "<catalog exercise id>" }`. Use dryRun: true to see the mapped tree before writing. ToS note: TrueCoach publishes no API and its terms prohibit third-party applications that interact with the service without written consent. This is a client-role read of the user's OWN data on their own account, run by hand; the coach has not been asked for consent yet. Do not present it as a sanctioned integration.

**Parameters**

- `from` — `string`, optional.
- `to` — `string`, optional.
- `programId` — `string`, optional.
- `dryRun` — `boolean`, optional.
- `mapping` — `object`, optional.
- `refresh` — `boolean`, optional.
