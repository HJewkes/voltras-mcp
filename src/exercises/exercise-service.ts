// Wave 1 stub for the exercise catalog wrapper.
//
// Task 08 (Wave 2B) replaces this stub with the real implementation that
// delegates to `searchExercises` and `getExerciseById` from
// `@voltras/workout-analytics`. It exists at this path now only so that
// `src/state/server-state.ts` can import the type during Wave 1 typecheck.
// Do NOT add behavior here — keep the surface stable so Task 08's diff is a
// straight body replacement.

export class ExerciseService {}
